[extend]
useDefault = true

[allowlist]
description = "Known non-secrets"
paths = [
  '''\.env\.example$''',
  '''\.env\.default$''',
  '''(^|/)(pnpm-lock\.yaml|package-lock\.json|poetry\.lock|uv\.lock)$''',
]
