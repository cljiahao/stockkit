export const PAGE_ROUTES = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  PRODUCTS: '/dashboard/products',
  LOGIN: '/login',
} as const;

export const API_ROUTES = {
  HEALTH: '/api/health',
} as const;
